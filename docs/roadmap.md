# Skyrim Content Workbench — 开发路线图

> 本文档为方向 C 确认后的重写版。旧版已归档至 `archive/`。
>
> 实现依据：`docs/spec.md`（产品规格）

---

## 阶段 0：项目初始化 ✅

> 完成于 2026-07-07，版本 v0.1.0。

目标：建立可运行、可测试、可打包的基础工程。

范围：
- [x] Python 3.12+ 项目初始化
- [x] PySide6 空窗口
- [x] SQLite 初始化
- [x] 分层目录结构（src/{app,domain,infrastructure,application}、tests、docs）
- [x] ruff 和 pytest
- [x] 基础日志
- [x] 应用数据目录创建

---

## 阶段 1：安全数据与文件操作基础 ✅

> 完成于 2026-07-07，版本 v0.2.0~v0.5.0。

目标：建立扫描、数据模型和安全移动能力，不先做复杂 UI。

范围：
- [x] 数据库 Schema v1（4 表 + Repository CRUD）
- [x] 领域模型（ModItem、FileAsset、FolderNode、OperationLog）
- [x] 只读文件扫描器（递归扫描 + 扩展名分类 + 中文路径）
- [x] Mod 条目组装服务（创建 ModItem、关联/移除成员、设角色、设封面）
- [x] 安全文件操作服务（移动预演 → 确认 → 执行 → 撤销）
- [x] 操作日志与撤销预演（B1/B2 安全校验）
- [x] 中文路径测试

> **注意：** 阶段 0 和阶段 1 的旧版代码（ModItem/FileAsset 体系）在新架构下需要逐步改造或重写。阶段 2 起所有新开发按新架构进行。

---

## 阶段 2：新骨架 + 基础浏览 ⏳

目标：建立新数据库模型，实现增量扫描和基础目录浏览。

### Task 1：新数据库 Schema + 迁移 ✅

> 完成于 2026-07-12，版本 v0.10.0。schema_version 由 3 升至 4。

- 创建新表：`content_unit`、`tag_category`、`tag`、`content_unit_tag`、`operation_history`、`folder_cache`
- 保留表：`managed_root`、`thumbnail_cache`（FK 由 `asset_id`→`file_asset` 改为 `content_unit_id`→`content_unit`）
- 移除旧表：`mod_item`、`file_asset`、`folder_node`、`operation_log`
- v3→v4 迁移函数，幂等
- 数据库位置：`{project_root}/data/app.db`（旧名 `SkyrimModWorkbench` 及 `%LOCALAPPDATA%` 路径已废弃）
- 应用数据目录统一置于项目根目录下的 `data/` 子目录

**验收：**
- [x] v3→v4 迁移创建全部新表且幂等
- [x] 旧表（mod_item、file_asset、folder_node、operation_log）已被移除
- [x] 保留表（managed_root）数据不受影响；thumbnail_cache 重建（FK 改为 content_unit_id）
- [x] 所有新表支持 Unicode 和中文路径
- [x] 迁移函数可重复执行（幂等）

### Task 2：新扫描器 ✅

> 完成于 2026-07-12，版本 v0.11.0。

**2026-07-13 设计修正**：扫描规则改为"所有压缩包文件自动标记为内容单元，文件夹不自动标记"。

- 保留 FileScanner 的递归扫描能力
- 增量扫描：启动时对比目录 mtime，跳过未变更目录
- 内容单元识别规则：所有压缩包文件（.7z/.zip/.rar 等）自动标记为内容单元
- 写入 FolderCache（目录树性能缓存）
- 候选内容单元写入 content_unit 表
- 跳过已被标记为内容单元的目录内部

**验收：**
- [x] 增量扫描基于 mtime 正确跳过未变更目录
- [x] 含压缩包的文件夹被识别为内容单元候选
- [x] 不含压缩包的分类目录不被错误识别
- [x] 扫描结果持久化到 content_unit 和 folder_cache
- [x] 支持中文目录名和文件名
- [x] 可手动触发全量重扫

### Task 3：目录树浏览 ✅

> 完成于 2026-07-12，版本 v0.12.0。

- 从 FolderCache 读取目录树结构
- 保留 FolderTreeModel 惰性加载架构（canFetchMore / fetchMore）
- 适配新数据源（folder_cache 表替代旧 folder_node）
- 目录树只展示基础分类（约一级或两级）
- 支持中文目录名

**验收：**
- [x] 目录树从 FolderCache 正确加载
- [x] 惰性加载正常（展开节点时查询子节点）
- [x] 中文目录名正确显示
- [x] 目录树不崩溃（错误节点降级为空子树）
- [x] 重启应用后目录树从缓存加载，无需重新扫描

### Task 4：文件列表 + 内容单元显示 ✅

> 完成于 2026-07-13，版本 v0.13.0。schema_version 维持 4。

**2026-07-13 设计修正**：中间列表从"仅显示内容单元"改为"显示目录下所有文件，内容单元只是标记"。
内容单元不是可见性门槛，未标记的文件也正常可见可操作。

- 选中目录 → 中间显示该目录下**所有文件和文件夹**（来自文件系统，非仅 content_unit 表）
- 已被识别为内容单元的项目 → 显示 [内容单元 ✓] 标记、封面缩略图（缩略图延后至阶段 4 Task 4）
- 未被识别为内容单元的项目 → 正常显示为普通文件/文件夹（文件名、类型图标、大小、日期）
- 右侧元数据面板（仅内容单元选中时显示元数据字段）
- 右键菜单对所有文件/文件夹可用（复制路径）

**验收：**
- [x] 选中目录 → 中间显示该目录下所有文件和文件夹（不仅是内容单元）
- [x] 内容单元正确显示标记 ✓
- [x] 非内容单元文件正常显示，与内容单元并列在同一列表中
- [x] 右键菜单可复制文件路径
- [x] 双击内容单元 → 右栏显示元数据
- [x] 双击非内容单元文件 → 不响应
- [x] 中文文件名正确显示
- [x] 路径文本自动省略（Elide），不撑大 UI 布局

### Task 5：双模式切换 + 扫描联动 ✅

> 完成于 2026-07-13，版本 v0.14.0。schema_version 维持 4。

- 顶部 [浏览|整理] 模式切换按钮
- 整理模式下中间区固定（暂存区展示），目录树变为目标选择器
- 扫描完成 → 目录树刷新 + 新候选内容单元出现在文件列表中

**验收：**
- [x] 浏览/整理模式切换正常工作
- [x] 整理模式下点击目录树节点不切换中间内容
- [x] 整理模式下目录树高亮显示选中的目标路径
- [x] 扫描完成后目录树自动刷新
- [x] 新扫描出的内容单元候选自动出现在文件列表中

---

## 阶段 3：暂存区 + 整理工作流 ✅

> 完成于 2026-07-17，版本 v0.20.0；Code Review 收尾修复 v0.20.1。

目标：实现从暂存区整理文件到分类目录的完整工作流。

### Task 1：暂存区标记与管理 ✅

- 右键目录 → "标记为暂存区" / "取消暂存区标记"
- 支持多个暂存区同时存在
- 暂存区在目录树中有视觉标记（如 [S] 图标或特殊颜色）
- 暂存区配置持久化到数据库

**验收：**
- [x] 右键目录可标记/取消暂存区
- [x] 多个目录可同时标记为暂存区
- [x] 暂存区在目录树中可辨识
- [x] 重启应用后暂存区标记保留

### Task 2：暂存区文件列表 ✅

> 完成于 2026-07-14，版本 v0.17.0。schema_version 维持 5。

- 选中暂存区 → 中间显示其下所有文件（含子目录中的文件）
- 文件列表显示：文件名、类型图标、大小、修改日期
- 支持排序：名称/日期/类型/大小
- 支持中文文件名

**验收：**
- [x] 选中暂存区显示其中所有文件
- [x] 文件信息正确（名称、类型图标、大小、日期）
- [x] 排序功能正常
- [x] 中文文件名正确显示

### Task 3：创建 Mod 组 + 手动修正 ✅

> 完成于 2026-07-14，版本 v0.18.0。schema_version 维持 5。
> 2026-07-15 修复：Nexus 命名规则适配 + 目录树/列表刷新 + 交互优化。
> 2026-07-16 修复：目录树刷新逻辑（parent_id 路径归一化 + 已删除目录清理）+ 双击内容单元文件夹进入目录 + 扫描后保持展开/选中状态。

- 右键文件 → "创建 Mod 组"
- 自动从文件名提取主要名称（支持 Nexus 下载命名 `Mod名称-数字ID-版本号-时间戳`）
- 下拉框提供选项：纯 Mod 名 / 完整原名
- 以所选名字在暂存区创建真实文件夹
- 将源文件移入新文件夹
- 新建内容单元，路径指向新文件夹
- **手动修正操作（扫描候选的纠错能力）：**
  - 右键未标记的文件/文件夹 → "标记为内容单元"
  - 右键已标记的内容单元 → "取消标记"（设为 "unmarked" 状态，扫描不重建）
  - 选中多个压缩包文件 → "把每个文件标记为内容单元"（各自独立标记）
- **2026-07-15 交互调整：**
  - 浏览模式双击文件夹 → 进入该目录（等价于目录树切换）
  - 单击选中内容单元 → 右侧立即显示元数据（详情面板交互方式）
  - 创建 Mod 组后目录树 + 中栏列表自动刷新
- **2026-07-16 目录树刷新修复：**
  - 创建 Mod 组后 folder_cache.parent_id 查找改用 `make_path_key` 归一化，避免大小写/分隔符差异导致新文件夹不可见
  - 扫描后清理已删除目录的 folder_cache 残留记录（对比 `all_visited_dirs` 与缓存记录，按路径深度降序删除避免 FK 约束冲突）
  - 浏览模式双击内容单元文件夹 → 进入该目录（优先于元数据显示，元数据通过单击查看）
  - 扫描/创建 Mod 组后保持目录树展开状态与选中节点（`save_expanded_paths` / `restore_expanded_paths`）

**验收：**
- [x] 右键文件弹出"创建 Mod 组"菜单项
- [x] 自动从文件名正确提取主要名称（Nexus 命名 + 通用命名）
- [x] 下拉框提供两种命名选项
- [x] 真实文件夹在暂存区中被创建
- [x] 源文件被移入新文件夹
- [x] 内容单元在数据库中被创建
- [x] 暂存区文件列表刷新，新文件夹出现
- [x] 创建 Mod 组后目录树自动刷新（新文件夹立即可见）
- [x] 整理模式列表中 Mod 组文件夹的子文件不显示（已收纳）
- [x] 浏览模式双击文件夹进入目录
- [x] 单击内容单元显示元数据
- [x] 创建 Mod 组后新文件夹立即出现在目录树中（无需重新扫描）
- [x] 资源管理器删除目录后重新扫描，目录树中旧节点被正确移除
- [x] 浏览模式双击内容单元文件夹进入该目录（而非显示元数据）
- [x] 扫描后目录树保持原有展开状态与选中节点

### Task 4：装配面板 ✅

- 创建 Mod 组后，中间内容区分割出"正在组装"子面板（仅整理模式）
- 面板显示当前选中 Mod 组文件夹的内容
- 通过暂存区文件列表右键菜单「加入装配」移入附加文件（汉化、预览图等）
  （2026-07-17 调整：取消原拖拽方案，改用右键菜单触发，底层业务逻辑不变）
- 支持移除已加入的文件（移回暂存区根目录，不保留原子目录结构）
- **不自动重命名图片**：自动整理阶段不修改任何文件名
- **手动重命名预览图**：装配面板中右键图片 → "重命名为与 Mod 组同名"
  - 单张：`{Mod组名}.{扩展名}`
  - 多张：`{Mod组名}_2.{扩展名}`、`{Mod组名}_3.{扩展名}`……
  - 命名冲突走现有 `ConflictError` 流程
- 整理模式下切换不同 Mod 组时，装配面板同步刷新内容
- 浏览模式不显示装配面板
- **装配面板切换交互（2026-07-17 调整）**：
  - 单击 Mod 组文件夹 → 仅选中显示元数据，不切换装配面板绑定（避免误触）
  - 双击 Mod 组文件夹 → 绑定装配面板到该 Mod 组并刷新内容

**验收：**
- [x] 创建 Mod 组后装配面板自动出现
- [x] 装配面板显示文件夹内已有文件
- [x] 整理模式下右键暂存区文件 → 「加入装配」可移动文件到 Mod 组文件夹
- [x] 加入装配后文件真实移动到 Mod 组文件夹，中栏列表与装配面板同步刷新
- [x] 未绑定 Mod 组时右键菜单不显示「加入装配」
- [x] 可移除已加入的文件（文件移回暂存区根目录）
- [x] 右键图片可手动重命名为 Mod 组同名
- [x] 多张图片重命名采用 _2、_3 后缀
- [x] 命名冲突时弹窗提示冲突（不覆盖，AGENTS 规则 2；覆盖/跳过/重命名选项留待阶段 5 通用冲突处理统一实现）
- [x] 整理模式下单击 Mod 组文件夹不切换装配面板（仅显示元数据）
- [x] 整理模式下双击 Mod 组文件夹切换装配面板绑定
- [x] 整理模式下切换 Mod 组，装配面板同步刷新
- [x] 浏览模式不显示装配面板

### Task 5：快速插入 ✅

- 整理模式下选中目标目录 → 点击「快速插入」按钮 → 当前装配面板绑定的 Mod 组整体移入目标目录
- 移动安全规则（spec §6.1）：确认弹窗、冲突处理（当前阶段拒绝 + 提示，覆盖/跳过/重命名留待阶段 5）、跨盘提示、子目录阻止
- 操作记录写入 operation_history
- ContentUnit.path 同步更新；folder_cache 旧路径记录清理

**设计决策（2026-07-17 用户确认）：**
- **取消拖拽方案**：原 roadmap 中"拖拽到目录树节点触发文件移动"和"目录树内拖拽重新分类"取消，与 Task 4 拖拽取消决策一致（Qt 拖拽反馈不理想）。仅保留「快速插入」按钮入口。
- **冲突处理范围**：Task 5 保持现有"冲突即拒绝"模式（ConflictError + 弹窗提示），覆盖/跳过/重命名选项留待阶段 5 通用冲突处理统一实现（AGENTS 规则 2：不覆盖）。
- **跨盘移动**：当前阶段直接拒绝并提示，不执行跨盘 move（Windows 跨盘 move 退化为 copy+delete，风险较高）。

**验收：**
- [x] 快速插入按钮在整理模式可见，浏览模式隐藏
- [x] 未绑定 Mod 组 / 未选中目标 / 目标与源相同时按钮禁用
- [x] 绑定 Mod 组 + 选中目标目录后按钮启用
- [x] 点击按钮弹出确认对话框（显示源路径 → 目标路径）
- [x] 用户确认后 Mod 组文件夹真实移动到目标目录
- [x] 移动后装配面板解绑 + 目录树刷新 + 暂存区列表刷新 + 状态栏提示
- [x] ContentUnit.path 同步更新到新路径
- [x] 重名冲突时弹窗提示（不覆盖，不执行移动）
- [x] 跨盘移动时弹窗提示（拒绝执行）
- [x] 文件夹移到自身或子目录时被阻止（按钮禁用 + 服务层二次校验）
- [x] 用户在确认对话框点击「否」时不执行移动
- [x] 每次成功操作写入 operation_history
- [x] 中文路径（Mod 组名 + 目标目录名）正常工作

### Code Review 收尾修复 ✅（v0.20.1，2026-07-17）

> Stage 3 正式 Code Review 后的收尾修复，不新增功能。详见 [CHANGELOG.md](../CHANGELOG.md) v0.20.1 与 [technical-debt.md](technical-debt.md)。

- [x] **TD-H7（H1）修复**：`list_by_path_prefix` 分隔符分歧漏匹配 → 新增 `list_by_path_prefix_normalized` 用 `make_path_key` 归一化比较，ContentService / QuickInsertService 所有调用点统一切换。
- [x] **TD-H8（H2）修复**：folder_cache 同步"吞异常 + 上层 commit"导致部分提交态 → 改为抛 `FileOperationError` 触发上层 `_rollback`，保证事务一致性。
- [x] **浏览模式双击导航 UI 状态保持修复**：`_on_entry_activated` 双击文件夹时同步 `tree_view` 选中节点，避免后续刷新逻辑误用陈旧的父目录节点导致中栏"退回"父目录。
- [x] **Technical Debt 第三批整理**：新增 10 项 TD（TD-M21 ~ TD-M27、TD-L18 ~ TD-L20），均含编号、背景、影响范围、推荐修复方案、建议修复阶段。截至 v0.20.1，无阻塞 Stage 4 启动的 High 级别技术债。
- [x] 测试：541 passed → 551 passed（+10：H1 回归 5 项 + H2 失败场景 2 项 + UI 状态保持 3 项）。

---

## 阶段 4：元数据 + 标签系统

目标：为内容单元建立完整的元数据标记和标签筛选能力。

### Task 1：标签分类管理 + JSON 导入导出

- 标签分类 CRUD（创建、读取、更新、删除）
- 每个分类有名称和色相值（color_hue）
- 预置 Skyrim 常用标签库（以 `default_tags.json` 文件形式随应用发布）：
  - 服装护甲：重甲、轻甲、法袍、现代、幻想、裸露、内衣、泳装、饰品、杂项、合集
  - 武器：单手剑、双手剑、弓、法杖
  - 作者：（空，用户自行添加）
  - 来源：N 网、L 网、韩网、群友分享、私货
  - 状态：已测试、已汉化、待测试
- 预制标签库加载逻辑：首次启动或标签表为空时自动从 `default_tags.json` 导入
- 标签管理对话框：
  - 「导入标签」按钮（QFileDialog 选 JSON → 加载到数据库）
  - 「导出标签」按钮（QFileDialog 选保存位置 → 导出当前标签库为 JSON）

**验收：**
- [x] 标签分类可增删改查
- [x] 颜色设置生效
- [x] 预置标签库正确加载（首次启动自动导入）
- [x] 导入 JSON 文件后标签库正确更新
- [x] 导出 JSON 文件包含所有分类和标签
- [x] 导出的 JSON 可重新导入

### Task 2：元数据编辑

> 打标签和元数据编辑操作同一个右栏面板，合并为一个 Task 一次性做完。
> 完成于 2026-07-19。schema_version 维持 6，无数据库迁移。
> **2026-07-19 设计决策**：用户确认 8 项决策（详见 CHANGELOG.md 2026-07-19 条目）。
> **2026-07-25 验收修正**：4 项问题修正（详见 CHANGELOG.md 2026-07-25 条目）。

- 编辑标题（中文显示名）、来源 URL（N 网链接可自动合成）、备注
- 给单个内容单元添加/移除标签（打字输入 + 自动补全已有标签 + 预选标签单击快速添加）
- 批量打标签：文件列表多选内容单元 → 右键 → "批量打标签" → 弹出对话框（打字自动补全 + 预选标签单击 + 添加/移除选择 + 显示已有标签 + 回车不关闭窗口）
- 支持中文标签
- 设置封面图片（从内容单元目录内选择图片 + 自动候选第一张）
- **2026-07-19 决策修正**：N 网 URL 自动合成本 Task 不实现（决策 3）；元数据保存采用显式「保存」按钮而非自动保存（决策 1）；整理模式完全隐藏右栏 MetadataPanel（决策 4 / 8）；标签输入采用 chip 列表 + 独立输入框（决策 6）；标签自动补全采用前缀匹配（决策 5）；封面默认选中第一张图片（决策 2）；保留 `metadata_text()` / `metadata_full_text()` 兼容旧测试（决策 7）。
- **2026-07-25 决策修正（推翻决策 4 / 8）**：原决策"整理模式完全隐藏右栏 MetadataPanel"在实测后发现右侧空白、无法释放空间，且用户反馈"装配模式下应能同时编辑元数据，避免切回浏览模式多此一举"。改为方案 B：整理模式保留 MetadataPanel。同时修正单击加载为主要入口（原双击入口改为兼容保留）；新增标签预选区域（输入框下方，单击快速添加到 chip）；BatchTagDialog 回车行为修正（仅添加到 chip 列表，不关闭窗口，最终点击「应用」才执行批量添加）。

**验收：**
- [x] 标题/来源/备注可编辑保存
- [x] 单个内容单元可添加/移除标签
- [x] 输入时自动补全已有标签
- [x] 批量选中内容单元后打标签生效于全部
- [x] 中文标签正常
- [x] 封面可从目录中选取
- [x] 第一张图片自动候选为封面
- [x] 重启应用后元数据保留
- [x] 单击内容单元加载 MetadataPanel（双击兼容保留）
- [x] 单击非内容单元清空 MetadataPanel
- [x] 标签输入框下方显示预选标签列表（排除已在 chip 的）
- [x] 单击预选标签快速添加到 chip
- [x] BatchTagDialog 回车仅添加到 chip，不关闭窗口
- [x] BatchTagDialog 点击「应用」才真正执行批量添加
- [x] 整理模式下右栏 MetadataPanel 保留可见（方案 B）

### Task 3：标签筛选

- 浏览模式下顶部标签筛选栏
- 先选标签分类 → 展开该分类下的标签
- 多选标签 → 内容单元列表实时筛选
- 多分类标签间为 AND 关系

**设计决策（2026-07-27 用户确认 Q1-Q8）：**
- Q1: B — 筛选激活时非内容单元条目全部隐藏，列表变成纯结果集
- Q2: A — 分类互斥展开（同时只展开一个分类）
- Q3: A — 切换目录树节点时筛选状态保留，自动应用于新目录
- Q4: A — 同分类 OR，跨分类 AND
- Q5: A — 默认全部折叠
- Q6: A — 筛选激活时保留元数据面板交互（不清空不隐藏，用户可继续查看选中条目的元数据）
- Q7: A — 选中标签使用边框高亮（不显示已选总数总览）
- Q8: A — 仅基础筛选，不做标签计数徽标

**验收：**
- [x] 标签筛选栏显示所有分类
- [x] 点击分类展开对应标签（互斥展开，再次点击折叠）
- [x] 多选标签后内容单元列表实时更新（同分类 OR）
- [x] 不同分类的标签组合筛选正确（跨分类 AND）
- [x] 无结果的空状态显示「无符合筛选条件的内容单元」
- [x] 筛选激活时非内容单元条目全部隐藏（Q1: B）
- [x] 选中标签边框高亮（Q7: A）
- [x] 折叠分类时分类按钮显示已选数徽标「分类名 (N)」
- [x] 「清除全部」按钮清空所有已选并恢复全量
- [x] 切换目录树节点时筛选状态保留（Q3: A）
- [x] 切到整理模式隐藏 TagFilterBar，切回浏览模式恢复且已选标签保留
- [x] 筛选激活时保留 MetadataPanel 交互（Q6: A：不清空不隐藏）
- [x] 标签管理对话框关闭后刷新 TagFilterBar（剔除已删除的已选标签）
- [x] TagService 未注入时 TagFilterBar 不创建（降级兼容）
- [x] 标签库为空时 TagFilterBar 隐藏

### Task 4：封面预览 ✅

- 内容单元列表中以小图标显示封面（FileListModel DecorationRole 注入 thumbnail_provider）
- 缩略图缓存（ThumbnailGenerator + ThumbnailService + ThumbnailCacheRepository，关联键 content_unit_id）
- 后台生成（QThread + Coordinator 模式，缓存命中同步返回）
- 启动 GC 清理孤立缓存（Q8:B）

**验收：**
- [x] 列表中小图标显示封面
- [ ] 大图卡片正确展示（暂未实现，仅小图标；大图卡片延后到下一阶段）
- [x] 无封面时显示占位（退化为 Qt 标准图标）
- [x] 缩略图缓存写入应用目录（`{project_root}/data/thumbnails/`）
- [x] 源图不被修改（Pillow 只读加载，缓存独立写入）

---

## 阶段 5：浏览增强 + 文件操作 + 交互优化

目标：完善浏览体验，补齐 Explorer 基础操作，优化高频交互效率。

### Stage 5 Task 拆分与顺序调整说明

**原 roadmap Task 3 拆分为 Task 3a / 3b / 3c 的原因**：
- 原 Task 3 包含 6+ 项独立子功能（新建文件夹、重命名、删除、复制/剪切/粘贴、任意目录间移动、应用外重命名检测），单 Task 体量过大违反"小而可审查"原则
- 应用外重命名检测（路径丢失）与文件操作 CRUD 关联弱，独立成 Task 3c
- 拆分后每个 Task 有明确边界，便于测试和回归

**原 roadmap Task 6（操作历史与撤销）提前到 Task 3a 之后的原因**：
- undo 框架是 Task 3b（复制/剪切/粘贴）和 Task 5（移动到对话框）的依赖
- 若 Task 6 放在最后，Task 3b/5 的文件操作需要后期补写 operation_history，违反"每个文件操作一开始就写历史"原则
- 提前实现 undo 框架后，后续所有文件操作直接复用

**最终 Task 顺序**：Task 0 → Task 1 → Task 2 → Task 3a → Task 6 → Task 4 → Task 3b → Task 5 → Task 3c → Task 7

### 已确认的设计决策（用户 2026-07-29 确认）

- **Q1（Windows 回收站实现）**：B — 采用 ctypes 直接调用 Windows `SHFileOperation` API，不引入 `send2trash` 第三方依赖
- **Q2（搜索索引选型）**：A — 使用 LIKE 直接查询，不引入 FTS5
- **Q3（剪贴板实现方式）**：A — 应用内剪贴板（不与系统剪贴板混用）
- **Q4（操作历史 UI 形式）**：A — QDialog 弹出
- **Q5（路径丢失检测）**：仅扫描时检测 + 状态栏提示 + 新增 "missing" status 值
- **Q6（大图卡片视图实现）**：B — 复用 `FileListModel`，避免数据源同步问题；如复杂度明显增加可再拆分
- **Q7（undo 不安全状态 UI 行为）**：C — 列表中预先标注「不可撤销」+ 点击撤销时弹窗提示原因
- **Q8（MainWindow 拆分策略）**：C — 边开发边小规模拆分，不单独开启大规模重构 Task

### Task 0：前置技术债修复 ✅

> 完成于 2026-07-29，版本 v0.30.1。为 undo 与文件操作做基础。

- [x] **TD-H1**：`OperationHistory.__post_init__` 增加 operation_type 与 target_path 一致性校验
- [x] **TD-L19**：`OperationHistory` delete 操作的 can_undo 必须为 False
- [x] **TD-M11**：`MainWindow._commit` 失败时通过 `QMessageBox.critical` 提示用户

### Task 0.5：数据目录路径抽象与隔离 ✅

> 完成于 2026-07-29，版本 v0.31.0。独立前置任务，不纳入 Task 1，避免缓存架构与路径迁移耦合。

**背景**：原应用数据目录固定在 `%LOCALAPPDATA%\SkyrimContentWorkbench\`，开发环境调试时数据混杂在系统 AppData 中不便管理。Task 1 的卡片视图缩略图缓存改造需要清晰的路径抽象，因此先独立完成数据目录迁移。

**路径决策优先级**：
1. `SCW_DATA_DIR` 环境变量（显式指定，生产环境用）
2. 项目根目录 `data/`（开发环境默认，通过向上查找 `pyproject.toml` 判定）
3. `%LOCALAPPDATA%\SkyrimContentWorkbench\`（Windows 回退）
4. `~/.skyrimmodworkbench/`（非 Windows 回退）

**安全约束（用户决策）**：
- 程序**不执行任何自动迁移、复制、删除操作**——数据安全由用户掌控
- 检测到旧 `%LOCALAPPDATA%\SkyrimContentWorkbench\` 有数据时，仅输出日志提示用户手动迁移
- 迁移完成后（新目录已有 `app.db`）不再提示
- 测试通过 `SCW_DATA_DIR` 环境变量严格隔离测试数据目录

**验收：**
- [x] 路径优先级正确：环境变量 > 项目根 data/ > AppData 回退
- [x] 开发环境默认使用项目根 `data/`，生产环境可通过 `SCW_DATA_DIR` 切换
- [x] 检测到旧目录时仅输出日志提示，**不执行任何文件操作**
- [x] 旧目录原样保留（不删除、不复制、不移动）
- [x] 新目录已有 `app.db` 时不再提示
- [x] 中文路径与含空格路径正常工作
- [x] 测试 fixture 通过 `SCW_DATA_DIR` 隔离，不污染项目 `data/`
- [x] 全量回归：1048 passed, 3 skipped, ruff check + format 全通过

### Task 1a：缩略图缓存架构改造 ✅

> 完成于 2026-07-29，版本 v0.32.0。Task 1b 的基础。

**背景**：Stage 4.5 的 64×64 单档 PNG 缓存对 Mod 资源浏览视觉价值有限。Task 1 卡片视图需要更大尺寸（256/512）的高清缩略图，因此先独立改造缓存架构。

**架构决策**：
- 取消 64 档封面缓存（64×64 对 Mod 资源浏览无视觉价值）
- 双档缓存：256（卡片视图基础）+ 512（右栏大预览 + 卡片视图大尺寸）
- 缓存格式从 PNG 改为 WebP (quality=90)，磁盘占用节省约 65%
- Schema v7 迁移：thumbnail_cache 复合主键 (content_unit_id, size)
- 512 档按需生成（点击卡片时触发），避免全覆盖的磁盘膨胀
- 旧 64 档记录保留，由 GC 清理无对应 content_unit 的记录

**验收**：
- [x] Schema v7 迁移成功且幂等
- [x] 旧 64 档记录保留（size=64）
- [x] 256/512 档独立缓存文件共存
- [x] WebP 格式输出正确
- [x] invalidate 清理所有档位 + 旧 PNG
- [x] GC 清理孤立 .webp/.png 文件
- [x] 全量回归：1054 passed, 3 skipped, ruff 全通过

### Task 1b：大图卡片 / 详细列表切换 + 在资源管理器中打开 ✅

> 完成于 2026-07-29，版本 v0.33.0（回归修复见 Unreleased）。
>
> 依赖前置：Task 0 扩展（v0.30.2）已实现标记文件夹时自动录入封面 + 右键「快速设置封面」，确保大图卡片视图有数据来源。Task 1a（v0.32.0）已完成双档 WebP 缓存架构，本 Task 完成 UI 适配。

**实现要点**：

- 新增 `CardListModel` 作为 `FileListModel` 的轻量代理（Q6:B 复用数据源），通过 `QStackedWidget` 切换 QTableView / QListView（IconMode）
- 有封面的内容单元在大图卡片视图显示封面（按 icon_size 动态选择 256/512 档缓存）
- 两种视图自由切换，切换不丢失选中状态（用 entry.path 匹配，Q4=A）
- 右键任意文件或目录 → "在资源管理器中打开"（调用 `explorer /select,` 定位并选中）
- 列表视图移除封面缩略图改用 Qt 标准图标（Task 1a 决策）
- 缩放滑块 128~512（默认 256），双击输入具体数值
- 右栏 MetadataPanel 封面预览 120×120 → 256×256

**验收：**
- [x] 有封面时大图卡片视图正确展示封面
- [x] 无封面时大图卡片视图显示占位图标
- [x] 详细列表视图行为不变（与 Stage 4 一致）
- [x] 两种视图自由切换，切换不丢失选中（含回归修复：卡片→列表方向，详见 Unreleased）
- [x] 右键 → "在资源管理器中打开" 可正常打开 Explorer 并选中文件
- [x] 全量回归：1055 passed, 3 skipped, ruff 全通过（含 `test_selection_preserved_card_to_list` 回归测试）

### Task 2：排序 UI ✅

> 完成于 2026-07-30。在阶段 3 Task 2 已有 `FileListModel.set_sort_key` 基础上补齐 UI 层可视化反馈与卡片视图排序入口。
> 2026-07-30 验收修复 2：排序下拉框最终方案 + UI 稳定性（详见 CHANGELOG.md Unreleased）。

**实现要点**：

- `FileListModel.headerData` 在当前排序列追加 ▲/▼ 方向指示（Q1=A 文本方案）
- `set_sort_key` 发射 `headerDataChanged` 刷新列头显示
- 视图切换栏新增排序字段下拉框 + 升降序方向按钮（Q2=A 列表/卡片视图共享）
  - 下拉框选项：名称 / 类型 / 大小 / 修改日期
  - 方向按钮显示 ▲/▼，点击翻转方向
- 列头点击与下拉框双向同步（点击列头后下拉框跟随，反之亦然）
- 前进/后退目录导航（用户验收时新增需求，类似资源管理器）
  - 维护浏览历史栈（back_stack + forward_stack + current_nav_path）
  - 仅浏览模式记录历史，整理模式不记录
  - 相邻相同路径去重，进入新目录清空前进栈（标准浏览器行为）
  - 历史导航触发的切换不再入栈，避免循环

**不实现**：
- 跨会话排序状态持久化（roadmap 明确"同会话内"）
- "状态"字段排序（Q3=A，遵循 roadmap 4 字段）
- 拼音排序（Q4=A，保持 Unicode 码点排序，零依赖）

**验收修复 2（2026-07-30 第二轮）**：
- 排序下拉框：双信号 deduplication 边界失效 → 回归单 `activated` 信号方案，放弃"选当前项重新排序"功能
- 排序下拉框 popup：取消当前项蓝色高亮（stylesheet 覆盖 `::item:selected`）
- 右栏元数据：`_path_value` / `_cover_value` 改用 `_ElidedLabel`（`QSizePolicy.Ignored` + ElideMiddle + ToolTip），解决长路径换行撑大右栏宽度
- 列表视图：新增 `_RubberBandTableView` 子类实现空白区域拖动框选（QTableView 不支持 `setSelectionRectVisible`）

**验收：**
- [x] 各排序字段正常工作（一次点击必生效）
- [x] 正序/倒序切换正确
- [x] 列头方向指示正确
- [x] 切换目录后排序状态保留
- [x] 中文名称排序合理（Unicode 码点顺序）
- [x] 卡片视图可通过下拉框切换排序
- [x] 前进/后退按钮在浏览多目录后可用
- [x] 后退后再进入新目录，前进栈清空
- [x] 整理模式不记录导航历史
- [x] 排序下拉框 popup 当前项无蓝色高亮
- [x] 右栏元数据路径/封面路径 elide 省略显示，右栏宽度不跳动
- [x] 列表视图空白区域拖动出现 rubber band 框选
- [x] 全量回归：1080 passed, 3 skipped, ruff check + format 全通过

### Task 3a：新建文件夹 + 重命名 + 删除 ✅

> 原 Task 3 拆分的第一部分，覆盖最基础的文件 CRUD。为 Task 6 undo 框架做铺垫。
> 完成于 2026-07-30。schema_version 维持 7，无数据库迁移。

- **新建文件夹**：目录树或文件列表右键 → "新建文件夹" → 输入名称 → 真实创建 + 同步 folder_cache
- **重命名**：右键重命名（真实改名 + 同步更新 content_unit.path）
- **删除**：右键删除（移至 Windows 回收站，采用 ctypes SHFileOperation，Q1: B）
- 三类操作均写入 operation_history（delete 的 can_undo=False，TD-L19 已校验）

**验收：**
- [x] 新建文件夹可创建并同步 folder_cache（含目录树右键菜单，验收 A 部分修复）
- [x] 重命名同时修改真实文件名和 content_unit.path
- [x] 删除操作将文件移至 Windows 回收站
- [x] 三类操作均写入 operation_history
- [x] 中文路径正常工作

### Task 6：操作历史与撤销 ✅

> 原 roadmap Task 6 提前到此位置，为后续 Task 3b/5 提供 undo 框架。
> 完成于 2026-07-30。schema_version v7 → v8 迁移。

- 操作历史对话框（QDialog 弹出，Q1: A 顶部工具栏按钮）展示 operation_history 表记录
- undo 框架：根据 operation_type 分派反向操作（move/rename/new_folder 可撤销，delete 不可撤销）
- undo 前校验文件状态安全，不安全时阻止（Q5: A：路径存在 + size/mtime 校验；Q7: A 二次确认弹窗）
- schema v8 迁移：operation_history 新增 undone_at 列 + operation_type='undo' 支持
- UndoService 写 undo 记录（can_undo=False，避免无限循环）+ mark_undone 标记原记录

**验收：**
- [x] 每次移动/重命名/删除操作记录到 history
- [x] 操作历史对话框正确展示记录列表
- [x] 可从历史选择操作撤销
- [x] 不安全状态（如源文件已被外部修改）阻止撤销并弹窗提示
- [x] 撤销后文件回到操作前位置
- [x] delete 操作在列表中标注「不可撤销」

### Task 4：键盘快捷键 ✅

> 完成于 2026-07-30，版本 v0.37.0。schema_version 维持 8，无数据库迁移。
>
> Task 4 在 Task 3b 之前完成，因此 Ctrl+C/X/V 当前为静默占位（Q4: C），待 Task 3b 实现后接入真实剪贴板逻辑。其余快捷键（F2 / Delete / Ctrl+Z / Ctrl+A）已完整可用。

- `F2`（中栏）：重命名选中条目（Q1: A 多选取第一个）
- `F2`（目录树）：重命名选中目录树节点（用户补充需求：目录树也需要重命名快捷键）
- `Delete`（中栏）：删除选中条目（移至回收站）
- `Ctrl+Z`（窗口级）：撤销最近一条可撤销操作（Q2: A 二次确认；Q3: B 跳过 delete/已撤销/undo 记录）
- `Ctrl+A`（中栏）：全选
- `Ctrl+C / Ctrl+X / Ctrl+V`（中栏）：静默占位（Q4: C），Task 3b 接入真实逻辑
- 快捷键上下文（Q5: A）：中栏 F2/Delete/Ctrl+A/Ctrl+C/X/V 为 WidgetShortcut，仅中栏聚焦生效；目录树 F2 为 WidgetShortcut，仅目录树聚焦生效；Ctrl+Z 为 WindowShortcut，任意位置聚焦均可触发
- F2 / Delete / 目录树 F2 仅在注入 FileOperationService 时注册；Ctrl+Z 仅在注入 UndoService 时注册；Ctrl+A / Ctrl+C/X/V 始终注册

**验收：**
- [x] F2 可重命名选中项（中栏 + 目录树）
- [x] Delete 可删除选中项
- [x] Ctrl+Z 可撤销最近操作（含二次确认 + 跳过 delete/已撤销）
- [x] Ctrl+A 可全选列表内容
- [x] Ctrl+C/X/V 占位不报错（待 Task 3b 实现）
- [x] 快捷键上下文范围正确（中栏/目录树/窗口级）
- [x] 未注入对应 Service 时快捷键不注册

### Task 3b：复制/剪切/粘贴 + 任意目录间移动 ✅

> 原 Task 3 拆分的第二部分，依赖 Task 6 的 history 框架。

- 应用内剪贴板（Q3: A），状态在 MainWindow 实例中
- 复制/剪切/粘贴真实文件操作 + 同步更新数据库
- 任意目录间移动（不限于暂存区→分类目录）
- 剪切状态下文件列表条目半透明显示（视觉提示）

**实际实现补充：**
- 新增 `ClipboardService`（应用内剪贴板，不与系统剪贴板混用，不持久化）
- 新增 `ConflictResolutionService` + `ConflictResolutionDialog`，粘贴冲突时弹窗让用户选择覆盖/跳过/重命名
- `FileOperationService.copy()` 新增，目录复制时同步复制 ContentUnit 元数据（新 id + 新 path）
- `FileOperationService.move()/copy()` 新增 `overwrite` 参数，支持覆盖前先删除目标
- 操作历史自动清理：`max_history_records=1000`，写入前预清理，保留可撤销记录
- 目录树支持全部快捷键（F2/Delete/Ctrl+C/X/V）及右键菜单（用户补充需求）
- schema_version v8 → v9，operation_history.operation_type CHECK 扩展 `'copy'`

**验收：**
- [x] 复制/剪切/粘贴正常工作且同步数据库
- [x] 任意目录间移动可用
- [x] 剪切状态有视觉提示（50% 透明度）
- [x] 操作写入 operation_history
- [x] 中文路径正常工作
- [x] 覆盖/跳过/重命名冲突解决正常
- [x] 操作历史自动清理，不造成数据库膨胀

### Task 5：「移动到……」快捷对话框 ✅

> 完成于 2026-07-30，版本 v0.39.0。
>
> 快速插入解决了暂存区→分类目录的场景，但常规文件管理还需要一个通用的"移动到任意目录"入口。

- 选中文件/文件夹 → 右键或快捷键触发 → 弹出目录树选择对话框
- 对话框内嵌 `FolderTreeModel` + 确认按钮
- 选目标目录 → 确认 → 文件/文件夹移动到目标位置
- 移动安全规则与现有保持一致（确认弹窗、冲突处理、跨盘提示、子目录阻止）

**设计决策（Q1-Q10 用户确认）：**
- Q1=A 多选支持 / Q3=B Ctrl+M 中栏+目录树 WidgetShortcut / Q4=A 中栏+目录树均添加右键菜单
- Q5=A 复用 ConflictResolutionDialog / Q6=B 不提供「新建文件夹」入口 / Q7=A 默认展开源父目录
- Q8=A 显示暂存区标记 `[S]` / Q9=A 对话框本身即确认，无二次确认

**实现说明：**
- 新增 [MoveToDialog](src/app/move_to_dialog.py)：独立 FolderTreeModel 实例（R2），源自身/子目录校验（R1），程序化测试接口
- [MainWindow](src/app/main_window.py) 集成中栏 + 目录树右键菜单、Ctrl+M 快捷键、`_perform_move_to` 复用 ConflictResolutionService 处理冲突
- 复用 Task 3b 的 FileOperationService.move（含 overwrite 参数）+ ConflictResolutionDialog，无新增 schema 迁移

**验收：**
- [x] 对话框展示完整的目录树（来自 FolderCache）
- [x] 选中目标后正确移动文件
- [x] 移动安全规则生效（源自身/子目录阻止、跨盘剪切拒绝、冲突覆盖/跳过/重命名）
- [x] ContentUnit.path 同步更新（FileOperationService._sync_on_move）
- [x] 操作写入 operation_history（FileOperationService.move 默认 can_undo=True）
- [x] 全量回归：1293 passed, 5 skipped, ruff 全通过

### Task 3c：应用外重命名/删除后的路径丢失检测与手动重新关联 ⏸️ 延期

> 原 Task 3 拆分的第三部分，独立功能。
>
> **状态：延期**（2026-07-30）
>
> **延期原因**：路径丢失检测与重新关联功能当前复杂度较高，涉及异常状态管理、
> UI交互和数据库维护，但实际使用频率较低，因此暂缓，未来根据实际需求重新评估。
>
> **已归档代码**：Task 3c 已完成开发（丢失检测 + 重新关联对话框 + 删除失效记录），
> 代码归档在 `stage5-task3c-suspended` 分支，主分支已回退至 Task 5 完成状态。
> 未来恢复开发时可基于该分支继续。

- 扫描时检测 ContentUnit.path 丢失（Q5: 仅扫描时检测）
- 状态栏提示「N 个内容单元路径丢失，点击查看」（Q5: 状态栏提示）
- ContentUnit.status 新增 "missing" 值（Q5: 新增 missing 状态）
- 重新关联对话框：显示丢失路径 + 文件选择器选新路径 + 更新 ContentUnit.path
- 删除失效记录（purge_missing）：best-effort 批量删除 + 二次确认

**验收（延期，待未来恢复后执行）：**
- [ ] 应用外重命名后扫描可检测路径丢失
- [ ] 状态栏提示用户路径丢失
- [ ] 重新关联对话框可手动选新路径
- [ ] 重新关联后 ContentUnit.path 更新且 status 重置
- [ ] 删除失效记录功能正常
- [ ] 中文路径正常工作

### Task 7：全局搜索

- 搜索范围：内容单元标题 + 标签名称 + 备注
- 使用 LIKE 查询（Q2: A），不引入 FTS5
- 支持中文和 UTF-8
- 搜索结果列表，点击跳转到对应内容单元所在目录

**验收：**
- [ ] 搜索标题/标签/备注命中正确结果
- [ ] 中文搜索正常
- [ ] 点击搜索结果跳转到内容单元所在目录

---

## 阶段 6：日后内容

> 目标：元数据交换、AI 辅助、打包发布等。当前需求尚不明确，暂不细化 Task。
> Stage 5 完成后，实际内容根据届时需求调整。

### 候选方向

- **元数据导入导出**：内容单元的标题、来源、备注、封面关联等元数据的 JSON 导入导出
- **AI JSON 交换**：导出未整理列表 → 网页 AI 分类建议 → 导入应用
- **Windows 打包**：PyInstaller 或 Nuitka
- **开源文档**：README、LICENSE、CONTRIBUTING
- **交互体验增强 / UI 美化**

---

## 版本约定

在 1.0.0 之前，0.MINOR.PATCH 中的 MINOR 用于标记阶段推进，PATCH 用于同阶段内的修复与小幅调整。

| 版本 | 对应 |
|------|------|
| v0.1.0 | 阶段 0 ✅ |
| v0.2.0~v0.5.0 | 阶段 1 ✅ |
| v1.0.0 | 阶段 6 完成后 |

旧版 v0.6.0~v0.9.0（阶段 2 旧架构）已不再使用。
