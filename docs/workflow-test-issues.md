# 实际工作流测试问题记录

> 预留文件（2026-08-02）。开始真实 Mod 库工作流测试后，在此记录发现的问题：
> 不符合预期的行为、操作不流畅的地方、崩溃/报错复现步骤等。
> 记录后按优先级整理为修复清单（建议格式：日期 / 现象 / 复现步骤 / 期望行为 / 优先级）。

## 26-8-2
### UI合理性1 ✅ 已修复（v0.50.2，2026-08-02）
- 装配面板需要改名字，现在功能与名称不太匹配
- 优先级L0
- 决策（用户确认 2026-08-02）：显示名改为「文件夹预览」——面板语义已是
  "文件夹内容透视"（绑定当前/钉住文件夹 → 显示内容 → 双击进入 / 右键操作 /
  拖入添加），"装配面板"与实际功能不符
- 实现：仅改 ui_constants 显示名，代码标识符（AssemblyPanel / assembly_*.py /
  ASSEMBLY_* 常量）保留 legacy 命名，待 UX 重构 Task 8 统一改名（见 TD-L30）

### UI合理性2 ✅ 已修复（v0.50.8，2026-08-03）
- 各处界面分割线，能通过拖动来移动的，默认比例需要重置
- 比如中栏，名称的宽度太短
- 操作历史里面的不能拖动，比例也不合理
- 并且需要有固化功能，重启保留
- 也需要重置功能
- 考虑放进UI重构版本
- 优先级L0
- 决策（用户确认 2026-08-03）：名称列加宽（中栏够宽不动）；快捷键项占位；
  重置布局连带清空操作历史列宽存档
- 实现：
  - 分割线状态抽成独立 helper SplitterStateHelper（QSplitter 尺寸 + QHeaderView
    列宽 保存/恢复/重置，QSettings 键 layout/*）；MainWindow 首次 showEvent 恢复、
    closeEvent 保存
  - 默认比例：主栏 220/480/324（中栏加宽）；文件列表名称列 Stretch、其余列
    默认 60/80/150；操作历史列 Interactive 可拖动 + 默认 180/340/90
  - 菜单「视图 → 重置布局」恢复默认并清除操作历史列宽存档（模态对话框下次打开生效）
- 验收反馈（2026-08-03）：文件列表四列改 Interactive 固定默认宽度
  （320/60/80/150，Explorer 风格右侧留白供框选）；列不随滚动条出现/消失横移，
  切换目录中栏不再跳一下
- 验收反馈（2026-08-03）：分割线固化失败根因——Windows 注册表（native 格式）
  把尺寸列表以字符串列表读回（如 ['300','420','300']），原校验只接受 int 导致
  恢复时回退默认；已兼容数字字符串，并改为拖动分隔线时实时保存（closeEvent 兜底）
- 验收反馈（2026-08-03）：中栏文件列表四列宽度同样接入固化——保存/恢复 +
  拖动即保存（键 layout/header/file_list），「重置布局」实时恢复默认宽度
- 手动控制宽度默认值位置（手动配置适应）
  全部集中在 [ui_constants.py](C:/AphrosyneData/Skyrim-Content-Workbench/src/app/ui_constants.py) 的「布局默认值」段：
  LAYOUT_MAIN_SPLITTER_DEFAULT_SIZES = (220, 480, 324) — 左/中/右三栏
  LAYOUT_RIGHT_SPLITTER_DEFAULT_SIZES = (625, 125) — 右栏元数据/装配面板
  FILE_LIST_COLUMN_WIDTHS = (0, 60, 80, 150) — 名称列是 Stretch（改它无效），改其余三列即可改变名称列分到的宽度
  LAYOUT_OPERATION_HISTORY_COLUMN_WIDTHS = (180, 340, 90) — 操作历史对话框三列

### UI合理性3 ✅ 已修复（v0.50.8，2026-08-03）
- 增添顶部菜单栏，跟随现代多数软件的UI设计
- 菜单栏点击展开菜单，可以进行功能选择或者进行界面跳转
- UI布局重置功能和快捷键分配功能放进这些地方
- 优先级L0
- 实现：
  - 新增 MainMenuBar 独立 view（构建动作并发信号，MainWindow 仅接线）
  - 「视图」：列表/卡片切换、重置布局（UI合理性2）、快捷键设置…（占位，
    自定义快捷键二期任务实现，见 UI合理性3 二期）
  - 「工具」：标签管理… / 操作历史…（按注入服务开关显隐）

### UI合理性4
- 增加不同后缀文件不同svg图标，用于快速区分文件类型
- 可能去使用单独一套全面的图标库
- 优先级L0

### 操作合理性1
- 双击文件有响应打开操作
- 打开压缩包、打开图片，用于快速预览
- 优先级L1

### 操作合理性2 ✅ 已修复（v0.50.7，2026-08-03）
- 图片可以直接预览
- 预计通过元数据封面位置，或是目前装配面板位置，直接显示图片
- 优先级L1
- 决策（用户确认 2026-08-03）：预览位置 = 元数据面板；直接加载原图、不做缓存
  （图片不会标记为内容单元，无需缓存）；封面优先（已标记图片单元设封面后显示封面）
- 实现：
  - 未标记图片文件 → 面板「图片预览」视图（隐藏编辑表单，标题/文件名/路径 + 原图）
  - 已标记图片文件单元无封面 → 封面预览区直接显示文件本身原图
  - 图片识别复用 ContentService.is_image_file（与封面候选扩展名集合一致）
- 验收反馈（2026-08-03）：元素自动靠顶（面板底部 stretch，消除元素间空行）；
  已有标签区高度改为鼠标拖动调整（默认恢复 240，范围 60~240，拖动条位于标签区下方）

### 操作便捷性1
- 可以右键一个文件夹进行一种 剥离 操作
- 直接提取文件夹内部文件到文件夹同级位置
- 简化 移动到 的快速操作
- 优先级L0

### 设计合理性1
- 一些使用率较低的右键功能，或者其他功能
- 可以通过菜单配置界面进行关闭
- 优先级L0

### UI合理性5 ✅ 已修复（v0.50.10，2026-08-03）
- 确实需要办法在列表视图区分文件夹类内容单元是否有封面
- 可以使用角标？等类似简单表述方式，而不是换图标
- 优先级L0
- 决策（用户确认 2026-08-03）：复用现有封面缓存图缩放到 64×64 作为图标，
  不产生新缓存、不加圆角
- 实现：文件夹内容单元有封面且缓存存在 → 列表图标显示封面（只读查询 256 档缓存）；
  缓存不存在回退标准文件夹图标

### 操作合理性3 ✅ 已修复（v0.49.0，2026-08-02）
- 删除文件夹的时候，顺便提示一下，文件夹内是否有文件，0个或者N个这样的
- 删除空目录的时候有信心
- 优先级L1
- 实现：删除确认文案在「此操作不可撤销。」前追加「（文件夹内含 N 个文件）」（统计选中文件夹顶层条目数，快速）

### UI合理性6 ✅ 已修复（v0.49.0，2026-08-02）
- 列表视图的大小列表，增加单位，和自动缩写
- 优先级L0
- 实现：大小列按 B / KB / MB / GB / TB 自动缩写（1024 进制，保留 1 位小数并去除尾随 0）；排序仍按原始字节值

### UI合理性7 ✅ 已修复（v0.49.0，2026-08-02）
- 重命名菜单默认宽度有点窄，可以适当提宽
- 优先级L0
- 实现：重命名弹窗宽度约为默认的 3/2

### 功能增加1
- 右键快速归档功能，标记归档目录功能
- 标记归档目录的文件夹，内再按日期分类
- 右键一个文件或者文件夹，快速归档，将文件或文件夹移动到归档目录，对应今天日期的文件夹内
- 比如今天是2026年8月2日，那么快速归档就移动到99_归档目录/2026-08-02目录中
- 并且取消归档目录内所有内容单元标记
- 并且扫描时跳过标记的归档目录
- 专门增加一个生成归档内容目录的功能，将当前文件夹下的内容都记录下来，记录外层文件夹和文件名字即可，不用向内搜索
- 优先级L1

### 操作便捷性2
- 中栏可以拖动到目录树
- 提供拖动放入目标高亮
- 优先级L2

### Bug紧急修复1 ✅ 已修复（v0.48.1，2026-08-02）
- 无法打开标签管理菜单
- 优先级L99

### UI合理性8 ✅ 已修复（v0.50.0，2026-08-02）
- 同分类标签排在一起，而不是名字排序，方便查找
- 优先级L0
- 实现：预选标签区域按分类分组显示，组内按名称排序（同分类天然相邻）

### 操作便捷性3 ✅ 已修复（v0.49.0，2026-08-02）
- 增加右键快速移动到上次目录，或者增加快捷键
- 现在整理内容，跨管理目录树移动，每次要移动到一个目录，弹出来的目录树菜单又得翻下去，展开折叠，然后才能选择，操作复杂度过高
- 或者有个替代方式，能减少我的工作量就行
- 优先级L3
- 实现（方案1：最近使用移动目标）：
  - 右键菜单「移动到...」后新增「移动到最近目录 ▸」子菜单（最近 5 个目标，点击直接移动）
  - Ctrl+Q 快捷键：中栏选中条目 → 直接移动到最近目标（默认快捷键暂定，后续做自定义快捷键菜单时开放配置）
  - MoveToDialog：顶部「最近目标」快捷按钮（点击直接确认）+ 打开时默认展开/选中最近目标
  - 记录最近 5 个成功移动目标，QSettings 持久化（跨会话保留，去重置顶）

#### 后续规划（第二期，未开始，2026-08-02 记录）
- **方案2：固定移动目标（书签）**：MoveToDialog 增加「固定此目录」按钮 + 右键固定列表
  （独立于最近记录，用户手动管理常用分类目录）
- **方案3：拖拽到目录树**（与「操作便捷性2」合并）：中栏文件/文件夹拖到目录树节点 =
  移动到该目录，含目标高亮、循环检测、跨盘与冲突处理；Qt 拖拽反馈历史上不佳，
  需单独验收
- **替代工作流**：现有「装配面板 📌 钉住 + 添加到钉住文件夹」已覆盖
  "高频目标 = 当前整理中的分类目录"场景，可作为过渡使用

### 操作便捷性4 ✅ 已修复（v0.50.0，2026-08-02）
- 标签每次添加标签自动保存，每次选完标签手动点一下保存可能遗忘
- 优先级L1
- 实现：标签改为即时保存——chip 添加/移除、预选点击、最近标签点击立即
  attach/detach + 提交；「保存」按钮仅负责标题/来源/备注/封面

### UI合理性9 ✅ 已修复（v0.50.0，2026-08-02）
- 现在标签越来越多，原来的已有标签栏要翻看好久才能找到需要的
- 是否有什么方案来解决，分类标签折叠？
- 还是类似上面的最近移动，提供右键打上最近标签
- 相关问题：UI合理性8、操作便捷性4
- 优先级L3
- 实现（方案确认 2026-08-02）：
  - 预选标签区域按 TagCategory 分类分组、分组头可折叠（默认展开，组内名称排序）
  - 「最近使用」标签区域（QSettings 持久化最近 10 个成功添加的标签，点击直接添加）
- 右键内容单元 → 「添加最近标签 ▸」子菜单，点击立即 attach + 提交
- 与操作便捷性4（即时保存）联动：所有标签操作即时落库
- 验收反馈补充修复（v0.50.0，2026-08-02）：
  - 元数据面板遮挡：预设标签区高度上限常量（METADATA_PANEL_PRESET_SCROLL_HEIGHT）
    此前只作上限不生效（面板底部 stretch 抢占剩余空间），已移除 stretch 使常量生效；
    小窗口下自动压缩到 60px，来源 URL / 备注不再被遮挡
  - chip 区标签：QListWidget 流式模式无边框、且 item 高度大于容器导致偏移/裁切，
    改为 FlowLayout 按钮（浅灰描边，与「最近使用/已有标签」一致）
  - 区域背景统一：chip / 最近使用 / 已有标签 = palette Base 深灰 + 4px 圆角
    （同色边框使圆角生效，视觉无边框线），与左栏目录树内部矩形一致
  - 装配面板：改为与左栏目录树同构的三层结构（1px 浅色外边框 → 窗体底色 →
    内部灰色圆角列表矩形）

### UI合理性10 ⏸️ 已记录（2026-08-02，待 UI 重构 / MainWindow 拆分时处理）
- 启动应用时短暂闪现多个小窗口后进入主窗口（标签点击闪窗已修复，见 UI合理性9 相关修复；
  启动闪窗仍存在，无 parent 容器排查后剩余疑似 Qt 内部控件瞬时创建）
- 处理时机：Phase 3 Task 8 UI 重构 / Task 7 拆分继续时，用截图/窗口枚举进一步定位

#### 第二期规划（未开始，2026-08-02 记录）
- 预选区域 / 标签管理加**搜索过滤框**（输入即过滤标签，比折叠更快）
- TagFilterBar 筛选标签同样支持搜索
- TagManagerDialog 分组管理增强（分类排序 / 拖动等）

### 操作合理性4 ✅ 已修复（v0.50.9，2026-08-03）
- 增加拖动功能后，原来的在文件内容上拖动多选的功能代码是否清理？
- 拖动选框功能，在列表视图和卡片视图都是，从下往上拉框无法选择，从右往左可以
- 优先级L0
- 根因：列表视图自定义 rubber band 的 `_select_rows_in_rect` 只处理"矩形边缘超出视口"
  的情况；在末行下方空白区起框（行数不满视口时从下往上拉）时
  `rowAt(rect.bottom())` 返回 -1 且视口内不处理 → 直接 return、选不中。
- 实现：
  - 矩形边缘落在视口内空白区（末行之下）→ 扩展到末行；整体在空白区则不选中
  - 文件列表四列改为 Interactive 固定默认宽度（Explorer 风格），右侧留出空白
    供框选；列不随滚动条出现/消失而横移（切换目录中栏不再跳一下）
  - 卡片视图使用 Qt 原生 rubber band，未改动（若仍异常为拖拽与框选冲突，另行评估）

### UI合理性11
- 元数据封面区域依旧不稳定，在480、512的最大值情况下，高度不稳定，横图和竖图消耗高度不同
- 导致已有标签栏被竖图挤压，300高度倒是没问题，不同类型的图消耗的高度都一致，
- 但是高度还是会抖一下（闪一下）
- 优先级L0

### 操作合理性5
- 右键创建MOD组可以继承元数据
- 优先级L1

### 操作便捷性5 ✅ 已修复（v0.50.10，2026-08-03）
- 增加一个筛选，筛选有封面和无封面
- 相关内容 UI合理性5
- 优先级L0
- 决策（用户确认 2026-08-03）：切换按钮（按下=只看有封面，未按下=全部），不持久化
- 实现：中栏标题栏「只看有封面」checkable 按钮，与标签筛选 AND 组合；
  无封面条目通过 64×64 封面图标直接识别

### 数据库问题1 ✅ 已修复（v0.50.1，2026-08-02）
- 重命名会在数据库中留存原本的条目？
- 我将一个文件AAA改成了BAA，搜索AA还是能搜到两个文件？
- 移动完成之后又多出一条搜索内容
- GC是否会接管及时清理？
- 我在重启两次后继续搜索，依旧是多条内容
- 优先级L3
- 根因（2026-08-02 已复现确认）：
  - 文件重命名/移动时，FileOperationService 只更新父目录 mtime，不更新
    content_unit 行（只有目录才会重写路径前缀）
  - 扫描持久化只插入新候选、从不删除失效行 → 旧路径残留 + 新路径新增一行；
    搜索匹配 title/notes/标签（title 默认=文件名），两行都命中 → 重复结果
  - 启动 GC 只清理孤立缩略图缓存，不碰 content_unit，重启无效
- 修复方案（用户确认 2026-08-02，已实现并验收）：
  - A：文件重命名/移动原地更新 content_unit 行（path/path_key + 默认标题跟随，
    用户自定义标题保留）
  - B：扫描时清理当前 root 下文件已不存在的 content_unit 行（直接删除，
    级联 content_unit_tag / thumbnail_cache；root 本身不存在时跳过整轮，防误删）
- 保守方案（备用，未启用）：删除前先生成待确认清单，用户确认后再删，
  避免误删仍带标签/备注/封面元数据的行；若后续发现直接删有误删案例再启用

### BugFix1 ✅ 已修复（v0.50.1，2026-08-02）
- 在目录树使用ctrl q快速移动到上一个目录，目录树没有刷新
- 批量快速移动似乎也没有准确刷新？也可能是提示移动失败的内容不会在目录树刷新，和Bug紧急修复2关联
- 优先级L2
- 根因（2026-08-02）：
  - Ctrl+Q handler 只读中栏选中，完全忽略目录树选中（Ctrl+M 反而有树版本，
    Ctrl+Q 漏了）→ 树上按 Ctrl+Q 无事发生，树自然不刷新
  - 批量移动后树不准确刷新 = Bug紧急修复2 的同一根因（失败项 folder_cache
    未同步 → 树重载后仍显示旧位置）
- 修复方案（用户确认 2026-08-02，已实现并验收）：
  - Ctrl+Q 改为目录树聚焦时移动树选中节点（_tree_selected_path），否则移动中栏
  - 移动后 _perform_move_to 统一 _refresh_tree

### Bug紧急修复2 ✅ 已修复（v0.50.1，2026-08-02）
- 批量移动内容时（包含文件文件夹），会有些提示移动失败，但是真实内容成功移动
提示：
```
移动到...

部分移动失败：成功11项，失败1项
移动失败：同步 folder_cache失败：无法删除 FolderCache:FOREIGN KEY
constraint failed

```
- 导致移动失败的内容，失去内容单元标记
- 和BugFix1关联
- 优先级L99
- 根因（2026-08-02 已复现确认，直接原因与数据库问题1 修复无关，为既有缺陷）：
  - on_folder_moved 只"删除旧单行 + 插入新单行"；被移动目录在 folder_cache 有
    子目录行（parent_id 引用）时，删除父行触发 FOREIGN KEY constraint failed
    （删除路径早有 delete_folder_subtree 先子后父，唯独移动路径没有）
  - 磁盘已移动但 DB 同步回滚 → 库仍指向旧路径 → 数据库问题1 的 Fix B 扫描清理
    把旧路径 content_unit 行当失效删除 → 文件夹标记（含标签/备注/封面）丢失；
    Fix B 之前这些行只是隐形残留，所以该症状是 Fix B 暴露出来的
- 修复方案（用户确认 2026-08-02，已实现并验收）：
  - on_folder_moved 改为整棵子树迁移（_move_folder_subtree）：新路径行父先子后
    插入（前缀重写、父链重建），旧行先子后父删除，不再 FK 失败
- 后续可选方案（Fix3，已记录 2026-08-02，暂未实施，可能后续增加）：
  - FileOperationService.move 同步失败时尝试把磁盘目标移回源位置（仅限移动前
    目标不存在、非覆盖场景，覆盖无法恢复）；回滚失败则错误信息明确"文件已实际
    移动但数据库未同步"，避免"提示失败但实际移动"后下次扫描清理丢元数据

### UI合理性12 ✅ 已修复（v0.50.9，2026-08-03）
- 批量打标签的界面也需要重构
- 参考UI合理性10
- 删除"（未添加标签）"
- 优先级L0
- 决策（用户确认 2026-08-03）：按推荐方案——预选标签按分类分组（组头可折叠）
  + 搜索过滤框（输入即过滤）；chip 区改 FlowLayout 按钮
- 实现：
  - 预选标签区：搜索框 + 按 TagCategory 分组（组头可折叠，组内按钮按名称排序）
  - 已选 chip 区：FlowLayout 按钮（「名称 ×」点击移除），删除「（未添加标签）」空提示
  - 预选/ chip 按钮边框按分类色着色（与 BugFix2 共用 tag_colors）
- 验收反馈（2026-08-03）：删除独立标签输入框（仅保留搜索框，搜索+点击即添加）；
  分组头不着色；标签按钮背景/边框统一分类色

### BugFix2 ✅ 已修复（v0.50.9，2026-08-03）
- 标签分类选颜色界面选的快速颜色和实际颜色不太一样
- 可能是暗色模式叠加导致的选色界面颜色都偏暗？
- 并且，目前分类颜色只有在标签管理界面才用上，存在感低
- 优先级L0
- 根因：QColorDialog 快速色为 RGB 色板，实现仅存 `hslHue()` 后按固定 S/L 重建显示，
  颜色必然不一致；暗色模式只是加重观感偏差
- 实现：
  - 选色对话框：预选色表（24 色相块，点击即选），替换 QColorDialog（所见即所得）
  - 新增共享 tag_colors helper（hue → QColor / hex / 色块图标）
  - 分类色接入标签管理树（去掉「（H=…）」调试文本）、元数据面板预选标签/chip、
    标签筛选栏、批量打标签
- 验收反馈（2026-08-03）：选色改为预选色表（24 色相块）而非单滑块；标签按钮
  背景/边框统一分类色、文字色按相对亮度自动黑/白；分组头不着色

### Schema 升级：tag_category 存储完整颜色（独立 issue，2026-08-03 记录）
- 背景：BugFix2 当前选色采用预选色表（24 色相），因数据库仅存 `color_hue`（0-360）
  单列；若需恢复全功能 QColorDialog（十六进制输入 / 自定义 RGB），必须存储完整颜色。
- 计划：bump `CURRENT_SCHEMA_VERSION`，`tag_category` 新增颜色列（RGB），
  同步 `TagCategory` 模型、仓库、`TagService.create_category / update_category_color`、
  标签 JSON 导入导出（兼容旧 `color_hue` 字段映射，旧导出可导入）。
- 前置条件：与「删除 content_unit.title 列」的 v14 迁移窗口协调（二选一占 v14，
  或分 v14/v15）；不影响当前预选色表方案（颜色一致已达成）。
- 优先级L1（体验增强，非阻塞）

### UI合理性13 ✅ 已修复（v0.50.5，2026-08-03）
- 内容单元标记应该在文件（夹）名字前面，防止文件（夹）名字过长导致遮挡
- 缩减内容单元标记，改成类似\[c\]一样的中括号+一个字母的标记
- 优先级L0
- 实现：列表视图标记改为名称前 `--`（`CONTENT_UNIT_MARKER` 改为 `--`，
  DisplayRole 拼接顺序反转；验收反馈 2026-08-03：`[c]` / `c-` 不够醒目，
  最终改 `--`）；
  卡片视图保持 Q6:B 决策不变（名称不含标记，ToolTip 承载状态）

### UI合理性14 ✅ 已修复（v0.50.6，2026-08-03）
- 元数据的标题留存选择
- 可以直接用重命名栏来代替
- 而不是数据库中又多一列
- 迁移可能比较麻烦，可以等后续数据导出导入时解决
- 优先级L0
- 决策（用户确认 2026-08-03）：保留 title 列但停止使用；UI 去掉标题输入框，
  创建/搜索/重命名不再用 title；schema 不动；原标题栏改为重命名栏位
  （回车保存，不走元数据保存按钮）；删除既有遗留别名（实查 4 条，以实际清单为准）。
- 实现：
  - MetadataPanel 标题输入框 →「重命名」栏：显示真实文件名，回车发射
    `rename_requested(unit_id, new_name)` → MainWindow 执行
    FileOperationService.rename（冲突/非法名走既有错误处理 + operation_history +
    目录树/中栏刷新）；「保存」按钮仅负责来源/备注/封面（封面已即时保存）；
    重命名成功后不重载表单（未保存的来源/备注编辑保留）
  - 创建（标记/扫描/Mod 组）不再写 title；`update_metadata` 移除 title 参数；
    重命名/移动不再维护 title
  - 搜索改为按真实文件名（basename）匹配，优先级 名称 > 标签 > 备注；
    搜索对话框列「标题」→「名称」，搜索范围仍仅限内容单元
  - 遗留别名清除：新增 `scripts/clear_legacy_titles.py`（默认 dry-run、幂等）+ 单测；
    真实库 4 条已清（title → NULL），1286 条默认 title 保留
  - schema 不动（CURRENT_SCHEMA_VERSION 仍 13）；删除 title 列见独立 issue

### Schema 升级：删除 content_unit.title 列（独立 issue，2026-08-03 记录）
- 背景：UI合理性14 收尾后，content_unit.title 已无任何读写路径（列保留但停止使用）。
- 计划：bump CURRENT_SCHEMA_VERSION 至 14，迁移删除 title 列（同步 ContentUnit 模型、
  content_unit 仓储列、相关测试；删除前确认旧库 title 数据无用户依赖）。
- 前置条件：等数据导出/导入机制就绪（原 UI合理性14 备注），且 title 停止使用经过
  一段真实使用期后再物理删除，避免迁移窗口冲突。
- 优先级L0（低风险但需迁移窗口，独立于 UI合理性14 处理）

## 26-8-3
### 操作便捷性6 ✅ 已修复（v0.50.3，2026-08-03）
- 封面设置也应当立刻保存而不是点保存按钮
- 错误成本低
- 优先级L0
- 实现：封面选择对话框点「确定」后立即调 `update_metadata` 落库并提交事务
  （面板新增 `apply_cover`，由 MetadataView 编排）；「清除封面」同样立即清空；
  「保存」按钮仅负责标题/来源/备注；封面保存不重载表单，未保存的
  标题/来源/备注编辑保留

### 操作便捷性7 ✅ 已修复（v0.50.10，2026-08-03）
- 双击进入一个文件夹，返回的时候应该记住选中的内容，防止一下子没找到
- 优先级L0
- 决策（用户确认 2026-08-03）：记住该目录最后一次选中（含多选）
- 实现：`_selection_memory`（目录 → 选中路径列表），后退/前进时按路径恢复并
  滚动到首个恢复行；导航前先更新当前路径避免恢复时记忆错归属目录

### UI合理性15
- 文件夹预览面板的位置应该换一个地方，目前从中栏看向右下角，眼球转动幅度大
- 优先级L0

### 操作便捷性8
- N网尾号自动填入网址栏
- 自动打开网址
- 优先级L0

### 操作便捷性9
- 快速浏览器搜索
- 增加前缀，例如 skyrim \[mod名字\]
- 下划线和横线自动替换成空格
- 优先级L0

### 功能增加2
- 外部工具，能在资源管理器里面直接快速重命名图片文件
- 多选两个文件的时候，图片文件快速重命名
- 优先级L0

### 操作便捷性10
- 文件夹预览支持快捷键
- 操作提示：可以利用文件夹预览固定，来固定需要频繁移入的文件夹
- 优先级L0

### UI合理性16 ✅ 已修复（v0.50.10，2026-08-03）
- 标签支持反选
- 相关内容 操作便捷性5
- 优先级L0
- 决策（用户确认 2026-08-03）：单个标签三态循环（点一下选中、两下反选、三下取消）；
  反选态边框=背景反色由降饱和替代；所有标签边框 2px
- 实现：未选=彩色圆角+亮度自适应字；已选=提饱和背景+白色描边环+加粗；
  已排除=降饱和背景+删除线；三态样式尺寸统一消除高度跳动；
  反选标签进入排除筛选（正选 AND 结果中剔除；可多个反选标签并存）
- 验收反馈（2026-08-03）：去掉 ✓/− 文字前缀（QT 非等宽导致宽度跳动），
  状态改由 加粗/删除线/颜色/描边 表达；分类按钮徽标改为「分类名 N」（去括号），
  并按加粗文字 + 空格两位数字预留宽度，状态切换不再跳动

### UI合理性17 ✅ 已修复（2026-08-03）
- 启用图片缓存机制，256大小，多内容下太卡
- 优先级L1
- 实现：恢复缩略图缓存链路到卡片视图（Stage 5 Task 1b 曾改为直接加载原图）：
  - 生成服务默认 cover 方形居中裁剪（生成器新增 `mode="cover"`，与 Task 2 验收
    视觉一致，无圆角/透明条）
  - 卡片未命中 → 固定尺寸占位图标（占地与缩略图一致，避免首次批量生成缓存时
    布局抖动）→ 后台生成完成自动刷新对应行
  - 缓存 256 档覆盖全部缩放预设（最大档即 256）；缓存失效/GC 复用既有链路

### 测试稳定性1 ✅ 已修复（v0.50.4，2026-08-03）
- 全量 pytest 在 `test_thumbnail_coordinator.py::test_thumbnail_ready_signal_emitted_after_generate`
  处原生崩溃（Windows fatal exception: access violation，缩略图 worker 线程调用
  `get_connection` 时与主线程竞争）
- 该文件单独运行（10 passed）及缩略图相关文件组合运行（38 passed）均通过；
  在干净 HEAD 检出上重跑全量同样崩溃 → 既有线程竞争问题，非 v0.50.3 引入
- 用户备注（2026-08-03）：可能与此前关闭缩略图机制有关——现在所有图片均用原图渲染
- 待后续排查：`thumbnail_worker` 线程与主线程 DB 连接竞争 / 测试中等待 worker 完成
- 优先级L2
- 根因（2026-08-03 已复现确认）：与缩略图 worker / DB 连接无关——是 `MetadataPanel`
  清除 chip / 预设 / 最近标签按钮时 `deleteLater` + lambda 闭包引用 `self` 的引用环：
  按钮 clicked/toggled 连接的 lambda 捕获面板，deleteLater 后若面板包装器已被回收，
  事件循环处理 DeferredDelete 时在按钮析构途中拆除连接 → 释放 lambda → 触发面板
  二次删除 → 原生崩溃（PySide6 6.11.1 + Python 3.14，access violation / Abort）。
  全量测试中恰好由 `test_thumbnail_coordinator.py` 的 QEventLoop.exec 处理挂起事件
  而暴露（worker 线程 sqlite3 帧仅为并发表象）。
- 最小复现子集：`test_metadata_panel.py::test_panel_load_none_clears`
  （或 `test_clear_panel_resets_all`）+ `test_thumbnail_coordinator.py`
- 实现（v0.50.4）：`MetadataPanel` 删除/清空按钮前先断开信号（clicked/toggled，
  新增 `_disconnect_button_signals` / `_disconnect_flow_buttons`，覆盖 chip / 预设 /
  最近标签与 `_remove_tag_chip` / `clear_panel` 等全部清理路径）；`TagFilterBar`
  rebuild 同样先断开；新增 2 个回归测试（旧 chip 信号已断开 + DeferredDelete 不崩溃）；
  全量 pytest 恢复稳定通过。

### 操作合理性6
- 操作历史支持undo redo
- 已经撤回(undo)的操作标记灰色而不是之间从历史删掉（至少目前在历史中看不到已经撤回的操作）
- 快捷键会自动跳过灰色的，只会从可撤回栈顶往里执行
- 优先级L0

### BugFix3
- 
