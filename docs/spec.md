# Skyrim Content Workbench — 产品规格说明

> 本文档为方向 C 确认后的重写版。旧版已归档至 `archive/`。
>
> 设计决策来源：`docs/product-review.md`（产品复盘）、`docs/design-workbook.md`（设计工作手册）

---

## 1. 项目定位

Skyrim Content Workbench 是一个面向 Windows 的、本地优先（local-first）的本地数字资产管理工具（DAM, Digital Asset Management）。第一阶段专门优化 Skyrim Mod 的整理与浏览工作流，但核心模型（内容单元 + 路径 + 元数据）可扩展到教程、截图、视频项目等其他资产类型。

它不替代 Mod Organizer 2、Everything 或 Windows 资源管理器。它解决的核心问题是：用户有大量零散放置的本地数字资产（Mod 文件、教程、素材、截图），需要通过预览、中文信息、标签和可交互的目录树，快速完成识别、标记、分类与归档。

项目计划开源，第一版不包含云同步、账号系统或用户数据上传。

---

## 2. 核心原则

### 2.1 模型原则

- **真实文件系统是唯一的事实来源（Single Source of Truth）**：文件始终是普通文件，能够脱离本程序继续被资源管理器、MO2 和其他工具使用。
- **数据库是元数据增强层**：数据库不是另一套文件组织体系，仅保存目录/文件自身无法表达的信息（中文显示名、标签、备注、来源 URL、封面关联、搜索索引、AI 分析结果）。
- **内容单元替代 ModItem + FileAsset**：数据库记录的是"内容单元"（一个文件夹或一个单文件），不是每个文件记录，也不是虚拟条目。
- **标签替代深层目录**：目录树保持扁平（建议最多二级分类），细分类由标签系统承担。

### 2.2 安全原则

- 所有文件操作直接作用于真实文件系统（不再是预演→确认→执行的模式），但保留必要的安全机制。
- 文件移动前弹出确认提示；重名冲突时弹窗让用户选择（覆盖/跳过/重命名）。
- 禁止将文件夹移动到自身或子目录。
- 跨盘移动需明确提示用户。
- 删除操作统一移至 Windows 回收站。
- 所有操作记录到简化版操作历史表，支持安全撤销。

### 2.3 中文优先

- 真实路径和文件名可为中文（MO2 对中文路径支持良好，无需回避）。
- ~~内容单元支持中文别名（title 字段），可选覆盖真实文件夹名。~~
  （UI合理性13，2026-08-03：title 列保留但停止使用，显示与搜索一律使用真实文件名；
  title 列物理删除列入独立 issue，待数据导出导入/迁移时机处理。）
- 所有 UI 优先使用中文显示。
- 搜索完整支持中文和 UTF-8。

### 2.4 技术原则

- Python 3.12+ / PySide6 / SQLite + FTS5 / Pillow
- local-first：所有数据保存在本机，不上传用户文件、路径、图片或元数据
- 不依赖统一命名规则：不假设 Nexus Mods、汉化包、社区分享文件或预览图存在可靠命名规律
- 不读写压缩包内部内容
- 不修改用户原图（缩略图缓存写入应用数据目录）

---

## 3. 第一版目标

第一版的目标是让用户能够完成一次完整的"暂存区整理 → 元数据标记 → 分类放入"工作流，并能够舒适地浏览和检索已有内容。

### 范围

1. 扫描用户指定的受管理根目录，通过增量扫描维护目录缓存。
2. 自动识别所有压缩包文件为内容单元候选，文件夹由用户手动标记。
3. 以基础分类目录树（一级或二级）展示资产库结构。
4. 提供单面板统一 UI：目录树 + 文件列表（列表/卡片视图 + 标签筛选 + 大图预览）+ 右栏（元数据 + 可钉住装配面板）。
5. 支持在任意目录内创建 Mod 组（自动提取名称 → 建文件夹 → 移入文件）。
6. 支持将文件添加到钉住文件夹（右键菜单或拖拽），以及文件夹/文件移动到目标分类目录。
7. 为内容单元设置中文别名、标签、来源 URL、备注和封面。
8. 预置 Skyrim 标签库，支持自定义标签，支持标签分类筛选。
9. 全局搜索（标题 + 标签 + 备注）。
10. 基础文件操作：重命名（同步更新路径）、删除（移至回收站）、复制路径。
11. 操作历史与撤销。
12. 导出待整理内容单元列表为 JSON，提供给外部网页 AI 分类建议。
13. 导入 AI 建议的 JSON，在本地审阅并确认后应用。
14. 缩略图缓存与预览图展示。

### 非目标

第一版不包含以下功能：

- 云同步、账号、多人协作或远程数据库。
- 替代 MO2 的安装、排序、启用、冲突管理或游戏启动功能。
- 替代 Everything 的全盘文件搜索。
- 不经用户确认的自动文件移动、删除、覆盖或重命名。
- 从 Nexus Mods 自动批量抓取预览图。
- 解析压缩包内部图片或内容。
- 文件哈希去重、完整文件监听、MO2 深度联动。
- 本地 AI 推理或 AI 直接控制文件系统。
- 非 Mod 类型的内容单元元数据管理（教程、截图等暂通过文件系统直接浏览，不入库）。

---

## 4. 核心数据模型

### 4.1 ContentUnit（内容单元）

数据库记录的核心对象。每个内容单元对应一个真实路径（文件夹或单文件）。

```text
id: str                    # UUID
path: str                  # 真实路径（可为中文）
title: str | None          # 已停用（UI合理性13）：列保留、不再写入，待 schema 升级删除
content_type: str          # "mod"（第一阶段仅此一种）
source_url: str | None     # 来源链接（如 N 网）
cover_path: str | None     # 封面图片路径（相对内容单元路径）
notes: str | None          # 备注
created_at: str            # ISO 8601 UTC
updated_at: str            # ISO 8601 UTC
```

### 4.2 TagCategory（标签分类）

标签的分组容器，用于 UI 筛选时的折叠/展开。

```text
id: str
name: str                  # 如"服装护甲"
color_hue: int             # 色相值，同一分类下标签颜色相近
```

### 4.3 Tag（标签）

```text
id: str
name: str                  # 如"重甲"
category_id: str           # 所属分类
```

一个标签只属于一个分类（一对一归属，不跨分类）。

### 4.4 ContentUnitTag（内容单元 ↔ 标签，多对多）

```text
content_unit_id: str
tag_id: str
```

### 4.5 OperationHistory（操作历史）

简化版操作记录，类似 PS 历史记录。

```text
id: str
operation_type: str        # move / delete / rename / new_folder
source_path: str
target_path: str | None
created_at: str
can_undo: bool             # 系统自动判断
```

### 4.6 ManagedRoot（受管理根目录）

保留现有设计。用于限定扫描范围。

### 4.7 FolderCache（目录树性能缓存）

简化版 folder_node，用于加速目录树显示。

```text
id: str
path: str
parent_id: str | None
last_scanned_mtime: float  # 上次扫描时的目录 mtime
created_at: str
```

### 4.8 ThumbnailCache（缩略图缓存）

保留现有设计。

### 4.9 已移除的旧实体

- ❌ ModItem → 由 ContentUnit 替代
- ❌ FileAsset → 不再以文件为粒度记录
- ❌ FileRole → 不再需要角色系统
- ❌ OperationLog（旧版四步状态机） → 由 OperationHistory 替代
- ❌ ConflictPolicy / OperationStatus / OperationType（旧版枚举）

---

## 5. 用户工作流

### 5.1 浏览模式（默认）

浏览模式的用途：浏览目录内容、查看和筛选内容单元、执行基础文件操作。

```
用户启动应用
    ↓
启动时自动扫描（增量扫描基于目录 mtime，跳过未变更目录）
    ↓
左侧目录树显示基础分类结构
    ↓
选中一个目录 → 中间显示该目录下的所有文件和文件夹
    ├─ 已被识别为内容单元的 → 显示 [内容单元] 标记和封面
    ├─ 未被识别为内容单元的 → 正常显示，无标记
    └─ 所有条目均可右键操作（复制路径、重命名、标记为内容单元等）
    ↓
在顶部标签栏筛选：
  ├─ 仅对已被标记为内容单元的项目生效
  ├─ 点击标签分类 → 展开标签 → 多选 → 列表实时筛选
  └─ 支持排序切换（名称/日期/类型/大小/状态 + 正序/倒序）
    ↓
单击内容单元 → 右栏立即显示元数据（详情面板交互方式）
双击文件夹（无论是否内容单元）→ 进入该目录（等价于目录树切换）
双击文件类型内容单元（压缩包）→ 显示元数据编辑面板
双击非内容单元文件 → 不响应
```

**关键设计**：
- 内容单元不是"可见性门槛"。文件系统里有什么，中间列表就显示什么。
  内容单元只是在文件基础上增加了**标记**和**元数据**，不影响文件本身是否可见。
- 元数据作为"详情面板"，单击查看更符合资源管理器/IDE/DAM 软件的交互习惯。
  文件夹的元数据通过单击查看；双击文件夹优先进入目录（而非显示元数据）。
- 扫描/创建 Mod 组等刷新操作后，目录树尽量保持原有展开状态与选中节点，
  避免用户每次都需要重新一级级展开。

### 5.2 整理流程（统一面板，原"整理模式"已移除）

整理用途：从零散文件创建 Mod 组、分类放入、重命名、删除。
UX 重构 Phase 1 已移除"浏览/整理"双模式切换与暂存区概念，所有操作在统一面板中完成。

```
【创建 Mod 组】（中栏右键，单/多选文件）
  ├─ 自动从文件名提取主要名称（剔除版本号、无用后缀等）
  ├─ 下拉框供选择：纯 Mod 名 / 完整原名
  ├─ 确认后以所选名字在当前目录创建真实文件夹，选中文件移入
  └─ 文件夹标记为内容单元
    ↓
【装配】
  ├─ 单击文件夹（内容单元或普通文件夹）→ 右栏下方装配面板透视其内部文件
  ├─ 📌 钉住装配面板 → 中栏右键"添加到钉住文件夹"或直接拖拽到装配面板
  ├─ 预览图可通过装配面板右键手动重命名为文件夹名
  └─ 装配完成
    ↓
【放入分类目录】
  ├─ 装配面板右键空白处 → "移动到……"（移动整个文件夹）
  ├─ 中栏/目录树右键 → "移动到……"（任意文件/文件夹移动）
  └─ 或复制/剪切 + 粘贴（跨目录）
    ↓
【直接操作】
  ├─ 中栏内拖拽文件到同目录文件夹 = 移入该文件夹
  ├─ 右键重命名（真实文件夹改名 + 同步更新 ContentUnit.path）
  ├─ 右键删除（移至回收站）
  └─ 右键复制路径
```

### 5.3 暂存区（已移除）

> 暂存区标记/列表功能已于 UX 重构 Phase 1 Task 1（v0.42.0 起）整体移除，
> 相关表（staging_area，schema v12 删除）与代码不再存在。
> 原"设置暂存区"流程（标记任意目录为暂存区）已废弃，不再支持。

### 5.4 扫描

```text
启动时自动增量扫描：
  - 遍历每个已知目录，对比当前 mtime 与上次扫描记录
  - mtime 未变 → 跳过
  - mtime 变化 → 重新扫描该目录及子目录

内容单元识别规则：
  所有压缩包文件（.7z/.zip/.rar 等）均自动标记为内容单元。
  文件夹本身不自动标记，用户可手动标记。
  手动标记文件夹为内容单元时，其内部的所有内容单元标记自动取消。

第一阶段仅识别 Mod 类型内容单元（含压缩包）。
教程、截图等类型的目录不作识别，直接通过文件系统浏览。

用户可随时手动触发全量重新扫描。
不做实时文件系统监听（避免 CPU 负载影响游戏）。
```

### 5.5 AI JSON 交换（第一版后期）

```text
[优先级靠后，先完成手动整理体验]

导出：
  - 选中未整理的内容单元 → 导出 JSON（含路径、文件名、大小、当前标签）
  - 可选导出已有分类目录结构

导入：
  - 导入 AI 返回的建议 JSON
  - 审阅差异（建议中文名、建议分类、建议标签）
  - 逐项或批量勾选确认
  - AI 只生成建议，不直接操作文件系统
```

---

## 6. 文件操作安全规则

### 6.1 移动

| 规则 | 策略 |
|------|------|
| 节点间移动 | 弹出确认提示，防止误操作 |
| 重名冲突 | 阶段 5 Task 3b 已实现通用冲突解决（ConflictResolutionDialog）：弹窗让用户选择 覆盖 / 跳过 / 重命名；选择覆盖时以 overwrite=True 先删除目标再执行（AGENTS 规则 2：不经确认不覆盖） |
| 跨盘移动 | 提示"跨盘移动暂不支持"并拒绝（当前阶段跨盘场景较少，且 Windows 跨盘 move 会退化为 copy+delete，风险较高） |
| 文件夹移到自身或子目录 | 阻止并提示（此操作不合理） |

### 6.2 删除

- 统一移至 Windows 回收站。
- 操作历史记录删除操作，实际撤销能力由回收站提供。

### 6.3 重命名

- 在应用内重命名：同时修改真实文件夹名和 ContentUnit.path。
- 在应用外重命名：原"路径丢失检测与重新关联"功能（Stage 5 Task 3c）已延期开发，
  代码归档于 `stage5-task3c-suspended` 分支，主线不包含；恢复前以重新扫描为准。

### 6.4 撤销

- 所有操作记录到 operation_history 表。
- 撤销时检查文件当前状态是否安全，不安全时阻止。

---

## 7. UI 结构

### 7.1 整体布局（统一面板）

UX 重构 Phase 1 起为单面板统一工作区，无"浏览/整理"模式切换：

```text
┌─── 顶部工具栏：搜索框 + 标签管理 + 操作历史 ───────────────┐
├──────────┬──────────────────────────┬─────────────────────┤
│          │                          │                     │
│  目录树   │    中栏：文件列表          │  右栏（3:2 分割）   │
│  （左侧） │    ├─ 标签筛选栏         │  ┌ 元数据编辑面板 ┐  │
│          │    ├─ 列表/卡片视图切换   │  │ 重命名/标签/来源/ │  │
│  AssetLib │    ├─ 排序/缩放/前进后退 │  │ 备注/封面/保存  │  │
│  ├─ Mods  │    └─ 刷新（F5）        │  ├────────────────┤  │
│  │├Armor  │                          │  │ 装配面板       │  │
│  │├Magic  │                          │  │（📌 钉住）     │  │
│          │                          │  └────────────────┘  │
└──────────┴──────────────────────────┴─────────────────────┘
└─── 状态栏（QStatusBar：扫描状态 / 提示）───────────────────┘
```

要点：
- 单击内容单元 → 右栏元数据面板；单击文件夹 → 装配面板透视其内部文件（可 📌 钉住）
- 所有文件操作（新建文件夹 / 重命名 / 删除 / 复制剪切粘贴 / 移动到……）在统一面板中可用
- 顶部无模式切换按钮（Phase 1 Task 1 移除）

### 7.2 内容浏览

- 点击目录树节点 → 中间显示该目录下的**所有文件和文件夹**
- 已被识别为内容单元的项目 → 显示内容单元标记（图标、封面）
- 未被识别为内容单元的项目 → 正常显示为普通文件/文件夹
- 右键菜单对所有项目可用（复制路径、重命名、删除）
- 未标记的文件/文件夹可右键 → "标记为内容单元"
- 已标记的内容单元可右键 → "取消标记"
- 顶部标签筛选栏：先选标签分类 → 展开标签 → 多选（仅过滤已标记的内容单元）
- 排序：名称 / 修改日期 / 类型 / 大小，正序/倒序切换
- 视图切换：详细列表 / 大图卡片（卡片尺寸 96~256 预选档），缩放不影响列表视图
- 前进/后退目录导航（类似资源管理器，同会话内）
- **内容单元不是可见性门槛**。所有文件系统内容都能在应用中看到和操作。
- **交互方式（2026-07-16 调整）**：
  - 单击选中内容单元 → 右栏立即显示元数据（详情面板交互方式）
  - 双击文件夹（无论是否内容单元）→ 进入该目录（等价于目录树切换）
  - 双击文件类型内容单元（压缩包）→ 显示元数据编辑面板
  - 双击非内容单元文件 → 不响应
- **展开状态保持**：扫描/创建 Mod 组等刷新操作后，目录树尽量保持原有展开状态与选中节点。

### 7.3 整理模式（已移除）

> "整理模式"及其中间区固定 / 目录树目标选择器行为已于 UX 重构 Phase 1 Task 1 移除。
> 整理流程并入统一面板，见 §5.2。「快速插入」按钮已于 Phase 1 Task 4 移除，
> 由「添加到钉住文件夹」+ 拖拽替代。

### 7.4 装配面板（文件夹透视器，右栏下方）

- 常驻右栏下方（元数据面板之下，3:2 分割），始终可见，未绑定时显示空状态占位
- 语义为"文件夹透视器"：单击文件夹（内容单元或普通文件夹）→ 显示其内部文件列表
- 📌 钉住：钉住后中栏操作不改变装配面板绑定；取消钉住后跟随中栏选中
- 文件添加：中栏右键「添加到钉住文件夹」（仅钉住时可见）或直接拖拽文件到装配面板
- 右键菜单继承中栏文件操作：重命名 / 复制 / 剪切 / 粘贴 / 移动到…… / 删除 / 复制路径
- **不自动重命名图片**：自动整理阶段不修改任何文件名，避免破坏用户已有命名
- **手动重命名预览图**：右键图片 → "重命名为与文件夹同名"，
  执行后图片重命名为 `{文件夹名}.{扩展名}`；多张图片时采用
  `{文件夹名}_2.{扩展名}`、`{文件夹名}_3.{扩展名}`……后缀；
  命名冲突走现有 `ConflictError` 流程（弹窗提示冲突，不覆盖，AGENTS 规则 2）
- 右键空白处 → "移动到……"：移动整个透视文件夹，成功后自动取消钉住
- 「加入装配」「移除文件」入口均已移除（由剪切/移动到/添加到钉住文件夹替代）

### 7.5 右键菜单

| 场景 | 菜单项 |
|------|--------|
| 普通文件 | 打开、重命名、复制、剪切、粘贴、删除、移动到……、复制路径 |
| 普通文件夹 | 打开、重命名、复制、剪切、粘贴、删除、移动到……、复制路径、在资源管理器中打开、钉住此文件夹 |
| 已标记的内容单元（文件） | 编辑元数据、设置封面、取消内容单元标记、移动到……、复制路径 |
| 已标记的内容单元（文件夹） | 编辑元数据、设置封面、取消内容单元标记、移动到……、复制路径、在资源管理器中打开、钉住此文件夹 |
| 装配面板内文件 | 重命名、复制、剪切、移动到……（与中栏一致） |
| 装配面板内图片 | 重命名为文件夹名、重命名、复制、剪切、移动到…… |
| 装配面板空白处 | 移动到……（移动整个透视文件夹）、取消钉住 |
| 多选（中栏） | 批量移动到……、创建 Mod 组、批量打标签 |
| 中栏/装配面板空白处（有钉住时） | 取消钉住 |

### 7.6 置顶按钮

> 置顶按钮已于 UX 重构 Phase 1 Task 1 随旧顶部栏一并移除，当前不再提供。

---

## 8. 搜索

第一版搜索范围：

- 内容单元文件名（真实文件名；UI合理性13 替代原 title 匹配）
- 标签名称
- 备注

搜索仅针对数据库中的内容单元元数据（记录存在即已标记）。第一阶段不搜索文件系统
和全盘文件（不搜索未标记条目）。

---

## 9. 预览图

- 内容单元可从其目录内选择一张图片作为封面。
- **封面自动候选**：默认自动选取内容单元目录内第一张图片作为封面候选，用户可确认或手动更换。
- 支持常见图片格式：JPG、JPEG、PNG、WEBP、GIF、BMP、TIF、TIFF、ICO。
- 应用生成的缩略图缓存保存到应用数据目录，不修改原始图片。
- 缩略图缓存有效性基于 `content_unit_id + source_size + source_modified_at`。
- 浏览模式下大图卡片展示封面；列表模式下小图标展示。
- 无封面的内容单元退化为文件列表样式。

---

## 10. 标签系统

### 10.1 标签分类结构

```text
TagCategory（标签分类）        Tag（标签）
服装护甲（蓝色系）             重甲, 轻甲, 法袍, 现代, 幻想, 裸露, 内衣, 泳装, 饰品, 杂项, 合集
武器（橙色系）                 单手剑, 双手剑, 弓, 法杖
作者（绿色系）                 （用户自行添加）
来源                           N网, L网, 韩网, 群友分享, 私货
状态                          已测试, 已汉化, 待测试
```

### 10.2 创建方式

- **预制标签库**：以 `default_tags.json` 文件形式随应用发布，首次启动或标签表为空时自动导入数据库。
- 用户可自定义新标签（打字输入 + 自动补全已有标签）。
- 支持将当前标签库导出为 JSON 文件（备份或批量修改后重新导入）。
- 可选 AI 扫描辅助建议。

### 10.3 UI 交互

- 筛选：先点分类 → 展开标签 → 多选高亮标签 → 筛选内容单元列表
- 打标签：打字自动补全（"最近常用置顶"未实现，已登记 TD-M30）
- 批量操作：选中多个内容单元 → 一次性添加/移除标签
- **标签管理对话框**：入口在浏览模式顶部或元数据面板旁，提供：
  - 标签分类和标签的增删改
  - 「导入标签」按钮（QFileDialog 选 JSON → 导入数据库）
  - 「导出标签」按钮（QFileDialog 选保存位置 → 导出为 JSON）

---

## 11. 数据库

### 11.1 新建表

```text
content_unit
tag_category
tag
content_unit_tag
operation_history
folder_cache
```

### 11.2 保留表

```text
managed_root（已有）
thumbnail_cache（已有）
```

### 11.3 移除表

```text
mod_item（旧）
file_asset（旧）
operation_log（旧）
```

### 11.4 Schema 版本

当前 schema v13（UX 重构 Phase 2 Task 6 已完成：移除 `is_marked` 字段，回归纯 DELETE 模式——
记录存在即已标记，取消标记 = DELETE 记录）。

---

## 12. 技术栈

| 层 | 技术 |
|----|------|
| UI | PySide6（Qt for Python） |
| 语言 | Python 3.12+ |
| 数据库 | SQLite + FTS5 |
| 缩略图 | Pillow |
| 测试 | pytest |
| 代码质量 | ruff |
| 打包 | PyInstaller 或 Nuitka（阶段 6） |

---

## 13. 阶段划分

详见 `docs/roadmap.md`。概览：

| 阶段 | 名称 | 状态 |
|------|------|------|
| 0 | 项目初始化 | ✅ 已完成（v0.1.0） |
| 1 | 安全数据与文件操作基础 | ✅ 已完成（v0.2.0~v0.5.0） |
| **2** | **新骨架 + 基础浏览** | ✅ 已完成（v0.10.0~v0.14.0） |
| **3** | **暂存区 + 整理工作流** | ✅ 已完成（v0.20.0~v0.20.1，暂存区功能后于 UX 重构 Phase 1 移除） |
| **4** | **元数据 + 标签系统** | ✅ 已完成（v0.21.0~v0.34.0） |
| **5** | **浏览增强 + 文件操作** | ✅ 已完成（v0.35.0~v0.41.0） |
| **6** | **日后内容** | ⬜ |

旧版架构（阶段 2 Task 1~4 的 ModItem/素材池/角色系统）已废弃，归档至 `archive/`。
